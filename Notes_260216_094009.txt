<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>صكّ | على خطى حراج - بيع وشراء موثق</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --haraj-green: #00897b; /* لون حراج الشهير */
            --haraj-bg: #f2f2f2;
            --text-main: #333;
            --primary: #1E3A8A;
            --accent: #D4AF37;
        }

        * { box-sizing: border-box; font-family: 'Cairo', sans-serif; }
        body { margin: 0; background: var(--haraj-bg); color: var(--text-main); }

        /* --- الهيدر (نمط حراج) --- */
        header {
            background: #fff; border-bottom: 2px solid var(--haraj-green);
            padding: 10px 5%; display: flex; justify-content: space-between;
            align-items: center; position: sticky; top: 0; z-index: 1000;
        }
        .logo-area { display: flex; align-items: center; gap: 10px; }
        .logo-img { width: 40px; height: 40px; border-radius: 8px; }
        .logo-text { color: var(--haraj-green); font-size: 1.5rem; font-weight: bold; }

        .search-bar { 
            flex: 1; margin: 0 20px; position: relative; max-width: 600px;
            display: flex; background: #eee; border-radius: 5px;
        }
        .search-bar input { 
            width: 100%; border: none; background: transparent; padding: 10px; outline: none;
        }
        .search-btn { background: var(--haraj-green); color: white; border: none; padding: 0 15px; border-radius: 0 5px 5px 0; cursor: pointer; }

        /* --- القائمة العلوية --- */
        .nav-menu { background: #fff; padding: 5px 5%; display: flex; gap: 15px; overflow-x: auto; border-bottom: 1px solid #ddd; }
        .nav-item { color: #666; text-decoration: none; font-size: 0.9rem; padding: 5px 10px; white-space: nowrap; cursor: pointer; }
        .nav-item.active { color: var(--haraj-green); font-weight: bold; border-bottom: 2px solid var(--haraj-green); }

        /* --- محتوى الصفحة --- */
        .main-content { padding: 20px 5%; max-width: 1100px; margin: auto; }
        .page-container { display: none; }
        .page-container.active { display: block; }

        /* --- قائمة الإعلانات (نمط حراج التقليدي) --- */
        .ad-item {
            background: #fff; padding: 15px; border-bottom: 1px solid #eee;
            display: flex; gap: 15px; cursor: pointer; position: relative;
        }
        .ad-item:hover { background: #fafafa; }
        .ad-thumb { width: 120px; height: 100px; border-radius: 8px; object-fit: cover; background: #eee; }
        .ad-info { flex: 1; display: flex; flex-direction: column; justify-content: space-between; }
        .ad-title { color: #0076a3; font-weight: bold; font-size: 1.1rem; text-decoration: none; }
        .ad-meta { font-size: 0.8rem; color: #888; display: flex; justify-content: space-between; align-items: center; }
        .price-tag { color: var(--haraj-green); font-weight: bold; font-size: 1rem; }
        .sakk-badge { background: #fff3cd; color: #856404; font-size: 0.7rem; padding: 2px 6px; border-radius: 4px; border: 1px solid #ffeeba; }

        /* --- زر إضافة إعلان العائم --- */
        .add-btn-fixed {
            position: fixed; bottom: 20px; left: 20px;
            background: var(--haraj-green); color: white; width: 60px; height: 60px;
            border-radius: 50%; display: flex; align-items: center; justify-content: center;
            font-size: 1.5rem; box-shadow: 0 4px 10px rgba(0,0,0,0.3); cursor: pointer; z-index: 1001;
        }

        /* --- مودال البيع (نظام العمولة) --- */
        .modal { position: fixed; inset: 0; background: rgba(0,0,0,0.7); display: none; align-items: center; justify-content: center; z-index: 2000; }
        .modal-content { background: white; padding: 20px; border-radius: 10px; width: 90%; max-width: 450px; }

        /* --- نظام الدردشة --- */
        .chat-ui { background: #fff; border: 1px solid #ddd; border-radius: 8px; height: 500px; display: flex; flex-direction: column; }
        .chat-msgs { flex: 1; overflow-y: auto; padding: 15px; display: flex; flex-direction: column; gap: 10px; background: #f9f9f9; }
        .msg { padding: 10px; border-radius: 8px; font-size: 0.9rem; max-width: 80%; }
        .msg.sent { background: #e1f5fe; align-self: flex-start; }
        .msg.received { background: #fff; border: 1px solid #eee; align-self: flex-end; }

        /* تحسينات الجوال */
        @media (max-width: 600px) {
            .ad-thumb { width: 80px; height: 80px; }
            .logo-text { font-size: 1.2rem; }
            .search-bar { display: none; } /* إخفاء البحث في الجوال لتوفير مساحة */
        }
    </style>
</head>
<body>

<header>
    <div class="logo-area">
        <img src="https://i.ibb.co/LzN2fP8/SAKK-ICON.png" class="logo-img" alt="SAKK">
        <span class="logo-text">صكّ</span>
    </div>
    <div class="search-bar">
        <input type="text" placeholder="ابحث عن سيارة، ساعة، عقار...">
        <button class="search-btn">بحث</button>
    </div>
    <div style="font-size: 0.8rem; color: #666;">مرحباً، راكان العوفي</div>
</header>

<div class="nav-menu">
    <div class="nav-item active" onclick="showTab('market')">الرئيسية</div>
    <div class="nav-item" onclick="showTab('chat')">الرسائل (🤖)</div>
    <div class="nav-item" onclick="showTab('post')">إضافة إعلان</div>
    <div class="nav-item" onclick="showTab('wallet')">أرباحي (1%)</div>
</div>

<div class="main-content">
    
    <div id="market" class="page-container active">
        <div id="adList">
            <div class="ad-item" onclick="openDeal(4100, 'آيفون 15 برو جديد')">
                <img src="https://via.placeholder.com/120x100?text=iPhone" class="ad-thumb">
                <div class="ad-info">
                    <div class="ad-title">آيفون 15 برو تيتانيوم - ضمان الوكيل</div>
                    <div class="sakk-badge">🛡️ موثق بصك رقمي: SAKK-9921</div>
                    <div class="ad-meta">
                        <span class="price-tag">4,100 ريال</span>
                        <span>قبل 5 دقائق | جدة</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div id="post" class="page-container">
        <div style="background: #fff; padding: 20px; border-radius: 8px;">
            <h3>إضافة موضوع جديد</h3>
            <input type="text" id="adTitle" placeholder="عنوان الإعلان">
            <input type="number" id="adPrice" placeholder="السعر">
            <textarea id="adDesc" placeholder="وصف السلعة..." style="width:100%; height:100px; padding:10px; margin-bottom:15px; border-radius:5px; border:1px solid #ddd;"></textarea>
            <button class="search-btn" style="width:100%; height:45px; border-radius:5px;" onclick="addAd()">نشر الآن</button>
        </div>
    </div>

    <div id="chat" class="page-container">
        <div class="chat-ui">
            <div style="padding:10px; border-bottom:1px solid #eee; font-weight:bold;">المفاوض الآلي نشط</div>
            <div class="chat-msgs" id="chatMsgs">
                <div class="msg received">مرحباً راكان، هل تريد شراء السلعة؟ السعر المعروض عادل جداً.</div>
            </div>
            <div style="padding:10px; display:flex; gap:5px;">
                <input type="text" id="chatInput" placeholder="رد على المفاوض..." style="margin:0;">
                <button class="search-btn" style="border-radius:5px;" onclick="sendChat()">إرسال</button>
            </div>
        </div>
    </div>

    <div id="wallet" class="page-container">
        <div style="background:white; padding:30px; text-align:center; border-radius:8px;">
            <div style="font-size:1.2rem; color:#888;">إجمالي أرباح راكان (عمولة 1%)</div>
            <div style="font-size:3rem; color:var(--haraj-green); font-weight:bold;" id="profitVal">0.00 ريال</div>
        </div>
    </div>

</div>

<div class="add-btn-fixed" onclick="showTab('post')">+</div>

<div id="payModal" class="modal">
    <div class="modal-content">
        <h3 style="color:var(--haraj-green)">إتمام الصفقة عبر صكّ</h3>
        <div id="payInfo" style="margin:15px 0; font-size:0.9rem; line-height:1.8;"></div>
        <button class="search-btn" style="width:100%; height:40px; border-radius:5px;" onclick="confirmPay()">تأكيد ودفع العموله</button>
        <button onclick="closeModal()" style="width:100%; margin-top:10px; background:none; border:none; color:#999; cursor:pointer;">إلغاء</button>
    </div>
</div>

<script>
    let profit = 0;

    function showTab(tabId) {
        document.querySelectorAll('.page-container').forEach(p => p.classList.remove('active'));
        document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
        document.getElementById(tabId).classList.add('active');
        event.currentTarget.classList.add('active');
    }

    function openDeal(price, title) {
        const fee = price * 0.01;
        document.getElementById('payInfo').innerHTML = `
            <b>السلعة:</b> ${title}<br>
            <b>السعر:</b> ${price} ريال<br>
            <b>عمولة الوساطة (1%):</b> <span style="color:red">${fee.toFixed(2)} ريال</span><br>
            <hr>
            <b>المبلغ الإجمالي: ${(price + fee).toFixed(2)} ريال</b>
        `;
        window.lastFee = fee;
        document.getElementById('payModal').style.display = 'flex';
    }

    function confirmPay() {
        profit += window.lastFee;
        document.getElementById('profitVal').innerText = profit.toFixed(2) + " ريال";
        alert("تمت الصفقة بنجاح! تم حجز عمولتك.");
        closeModal();
    }

    function closeModal() { document.getElementById('payModal').style.display = 'none'; }

    function addAd() {
        const t = document.getElementById('adTitle').value;
        const p = document.getElementById('adPrice').value;
        if(!t || !p) return;
        
        const adHtml = `
            <div class="ad-item" onclick="openDeal(${p}, '${t}')">
                <img src="https://via.placeholder.com/120x100?text=NEW" class="ad-thumb">
                <div class="ad-info">
                    <div class="ad-title">${t}</div>
                    <div class="sakk-badge">🛡️ موثق بصك رقمي: SAKK-${Math.floor(Math.random()*9000)}</div>
                    <div class="ad-meta">
                        <span class="price-tag">${p} ريال</span>
                        <span>الآن | من قبلك</span>
                    </div>
                </div>
            </div>
        `;
        document.getElementById('adList').insertAdjacentHTML('afterbegin', adHtml);
        showTab('market');
    }

    function sendChat() {
        const inp = document.getElementById('chatInput');
        if(!inp.value) return;
        document.getElementById('chatMsgs').innerHTML += `<div class="msg sent">${inp.value}</div>`;
        inp.value = "";
        setTimeout(() => {
            document.getElementById('chatMsgs').innerHTML += `<div class="msg received">🤖 المفاوض الآلي: هل تود تثبيت سعر العرض في الصك الرقمي؟</div>`;
        }, 1000);
    }
</script>
</body>
</html>